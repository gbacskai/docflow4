version: 1
backend:
  phases:
    preBuild:
      commands:
        - 'echo "🔄 Pulling latest backend environment configuration..."'
        - 'echo "🎯 Current AWS_BRANCH: $AWS_BRANCH, ENV: $ENV"'
    build:
      commands:
        - nvm install 24.5.0 --no-progress
        - nvm use 24.5.0
        - npm ci --cache .npm --prefer-offline
        - 'echo "🏗️ Building with ENV=$ENV, AMPLIFY_BRANCH=$AMPLIFY_BRANCH"'
        - npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 24.5.0 --no-progress
        - nvm use 24.5.0
        - npm ci --cache .npm --prefer-offline
        - 'echo "📦 Frontend pre-build with ENV=$ENV"'
        - npx ampx generate outputs --branch $AWS_BRANCH --app-id $AWS_APP_ID
    test:
      commands:
        - nvm use 24.5.0
        - npm run test:headless
    build:
      commands:
        - nvm install 24.5.0 --no-progress
        - nvm use 24.5.0
        - 'echo "🔨 Frontend build with ENV=$ENV"'
        - npm run build
  artifacts:
    baseDirectory: dist/docflow4/browser
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*
      - node_modules/**/*
env:
  variables:
    NODE_VERSION: 24.5.0
    _LIVE_UPDATES: '[{"name":"Amplify CLI","pkg":"@aws-amplify/cli","type":"npm","version":"latest"}]'
    ENV: $AWS_BRANCH
    AMPLIFY_BRANCH: $AWS_BRANCH
    AMPLIFY_ENVIRONMENT_NAME: $AWS_BRANCH
